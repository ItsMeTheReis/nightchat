# NightChat — dependências por fase.
# FASE 1 (cliente terminal): NENHUMA dependência externa — apenas a stdlib do Python.

# --- Fase 2 (servidor relay + cliente conectado) ---
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12.0
sqlalchemy>=2.0
psycopg[binary]>=3.1              # driver PostgreSQL (produção)
argon2-cffi>=23.1
python-jose[cryptography]>=3.3    # tokens de sessão (JWT)
python-dotenv>=1.0
pydantic>=2.6

# --- Fase 3 (identidade criptográfica Ed25519) ---
pynacl>=1.5

# --- Fase 4 (handshake X25519 + HKDF) ---
cryptography>=42.0              # HKDF-SHA256

# --- Fase 5 (AEAD por mensagem — ainda NÃO implementado) ---
# (XChaCha20-Poly1305 via pynacl, já presente acima)

# --- Fase 7 (testes) ---
pytest>=8.0
httpx>=0.27                       # exigido pelo fastapi.testclient.TestClient

# --- Fase 8 (empacotamento .exe) ---
# pyinstaller>=6.0
